Riot Vanguard Guard v3

這版會把 guard.log 放在 Guard 自己所在的資料夾。

安裝：
1. 解壓縮到你打算永久放置的位置。
2. 雙擊「安裝.cmd」。
3. UAC 按「是」。
4. guard.log 會出現在同一個資料夾。

重要：
安裝後不要移動或重新命名這個資料夾，因為 Windows 工作排程器會記住 RiotVanguardGuard.ps1 的完整路徑。

移除：
雙擊「移除.cmd」。
移除排程工作，但不會刪除腳本或 guard.log。
